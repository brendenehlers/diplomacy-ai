# Virtualenv
.venv/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Game run artifacts
runs/

# Secrets
.env

# Local copy of the bundled diplomacy web UI (see `just ui-setup`)
ui/

# diplomacy websocket server runtime state (see `just serve-engine`)
data/
